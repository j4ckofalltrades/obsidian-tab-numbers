# Changelog

## 0.1.3

- Replace `builtin-modules` with Node.js built-in `node:module`
- Replace `lint-staged` with `nano-staged`
- Add automated release workflow with artifact attestations
- Bump dependencies

## 0.1.2

- Fix README title to match plugin name

## 0.1.1

- Fix missing `version` field in `manifest.json`

## 0.1.0

- Initial release
- Displays numbered badges (1–8) on tabs when Ctrl/Cmd is held
- Badge text and background color customizable via settings
